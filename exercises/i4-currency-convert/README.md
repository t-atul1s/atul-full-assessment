# I4 — Currency Convert (FastAPI + Node CLI)

## API

```bash
cd api && pip install -r requirements.txt && uvicorn main:app --port 8000
pytest tests/ -v
```

## CLI

```bash
cd client && node cli.js --amount 100 --source USD --target INR
```
